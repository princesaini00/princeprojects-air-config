server:
  port: 5002

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/airline_db
    username: myprojects
    password: myprojects
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true