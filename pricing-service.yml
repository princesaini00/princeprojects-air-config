server:
  port: 5011

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/airline_pricing_db
    username: myprojects
    password: myprojects

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true